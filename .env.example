# URL of the Google Apps Script Web App deployment that writes to the
# free-stuff subscribers Google Sheet. See free-stuff/google-apps-script/README.md
# for setup steps. This is called directly from the browser (see
# src/lib/claimResource.ts) — there is no server-side secret to protect here,
# only the deployed Web App URL.
VITE_APPS_SCRIPT_URL=
